package outcobra.server.util

class ProfileRegistry {
    companion object {
        const val PROFILE_MOCK_SERVICES = "mock_services"

        const val DISABLE_AUTH_FILTER = "disable_auth_filter"
        const val BASIC_AUTH = "basic_authentication"
        const val AUTH0_AUTH = "auth0_authentication"
    }
}